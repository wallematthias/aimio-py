[build-system]
requires = [
  "setuptools>=61.0",
  "wheel",
  "pybind11>=2.6",
  "numpy>=1.20,<2.3; python_version < '3.11'",
  "numpy>=1.20; python_version >= '3.11'",
]
build-backend = "setuptools.build_meta"
