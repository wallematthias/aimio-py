name: Build Wheels

on:
  pull_request:
    branches: [ "main", "master" ]
  push:
    branches: [ "main", "master" ]
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build_wheels_native:
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, macos-15-intel]
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Miniconda
        uses: conda-incubator/setup-miniconda@v3
        with:
          activate-environment: buildenv
          python-version: ${{ matrix.python-version }}
          channels: conda-forge
          auto-activate-base: false

      - name: Log Runner Architecture
        shell: bash -l {0}
        run: |
          python - <<'PY'
          import platform
          print("platform.machine():", platform.machine())
          PY

      - name: Assert macOS ARM Runner
        if: matrix.os == 'macos-latest'
        shell: bash -l {0}
        run: |
          python - <<'PY'
          import platform
          arch = platform.machine().lower()
          if arch != "arm64":
              raise SystemExit(f"Expected arm64 on macos-latest, got: {arch}")
          print("Verified macos-latest runner arch:", arch)
          PY

      - name: Install build dependencies
        shell: bash -l {0}
        run: |
          conda install -y \
            boost-cpp \
            pybind11 \
            numpy \
            pip \
            setuptools \
            wheel
          python -m pip install --upgrade build

      - name: Build wheel
        shell: bash -l {0}
        run: |
          python -m build --wheel --no-isolation

      - name: Validate macOS Wheel Tag (arm64)
        if: matrix.os == 'macos-latest'
        shell: bash -l {0}
        run: |
          ls -1 dist/
          ls dist/*.whl | grep -E 'macosx_.*_arm64\.whl$'

      - name: Upload wheel artifact
        uses: actions/upload-artifact@v4
        with:
          name: wheels-native-${{ matrix.os }}-${{ runner.arch }}-py${{ matrix.python-version }}
          path: ./dist/*.whl

  build_wheels_linux:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install cibuildwheel
        run: |
          python -m pip install --upgrade pip cibuildwheel

      - name: Build Linux wheels (manylinux)
        env:
          CIBW_BUILD: "cp310-manylinux_x86_64 cp311-manylinux_x86_64 cp312-manylinux_x86_64 cp313-manylinux_x86_64"
          CIBW_SKIP: "*-musllinux_*"
          CIBW_BEFORE_ALL_LINUX: >-
            if command -v yum >/dev/null 2>&1; then yum install -y boost-devel;
            elif command -v dnf >/dev/null 2>&1; then dnf install -y boost-devel;
            elif command -v apt-get >/dev/null 2>&1; then apt-get update && apt-get install -y libboost-all-dev;
            else echo 'No supported package manager found for Boost install' && exit 1; fi
        run: |
          python -m cibuildwheel --platform linux --output-dir dist

      - name: Upload Linux wheel artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wheels-manylinux-x86_64
          path: ./dist/*.whl

  build_sdist:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build sdist
        run: |
          python -m pip install --upgrade pip build
          python -m build --sdist --outdir dist

      - name: Upload sdist artifact
        uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: ./dist/*.tar.gz

  publish:
    needs: [build_wheels_native, build_wheels_linux, build_sdist]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write
      contents: read
    environment:
      name: pypi
      url: https://pypi.org/p/aimio-py
    steps:
      - name: Download wheel artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          path: dist
          merge-multiple: true

      - name: Download sdist artifact
        uses: actions/download-artifact@v4
        with:
          name: sdist
          path: dist

      - name: Set up Python for debug tooling
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Debug artifact inventory
        run: |
          echo "Current dist/ contents:"
          ls -lah dist
          echo
          echo "Wheel files:"
          ls -1 dist/*.whl || true
          echo
          echo "Source distributions:"
          ls -1 dist/*.tar.gz || true

      - name: Debug wheel tags
        run: |
          python - <<'PY'
          from pathlib import Path
          wheels = sorted(Path("dist").glob("*.whl"))
          if not wheels:
              print("No wheels found in dist/")
          for w in wheels:
              print(w.name)
              if "linux_x86_64" in w.name and "manylinux" not in w.name and "musllinux" not in w.name:
                  raise SystemExit(f"Non-portable Linux wheel tag detected: {w.name}")
          PY

      - name: Twine check (debug)
        run: |
          python -m pip install --upgrade pip twine
          python -m twine check dist/*

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          verbose: true
